name: iOS Beta Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  # 固定 sing-box 版本号，避免上游发新版把你的 CI 跑挂
  SING_BOX_VERSION: v1.11.0

jobs:
  # ------------------------------------------------------------------
  # Job 1: 编译 sing-box 的 Libbox.xcframework (采用压缩归档保存)
  # 只有 SING_BOX_VERSION 变化时才会重新编译（靠 cache key 命中判断）
  # ------------------------------------------------------------------
  build-libbox:
    runs-on: macos-14
    timeout-minutes: 30
    outputs:
      cache-hit: ${{ steps.cache-libbox.outputs.cache-hit }}
    env:
      CGO_ENABLED: 1
      GOFLAGS: -ldflags=-linkmode=external
    steps:
      # 【修改处 1】：将缓存路径改为 zip 压缩包，彻底防止跨环境损坏
      - name: Cache libbox archive
        id: cache-libbox
        uses: actions/cache@v4
        with:
          path: libbox-output/Libbox.xcframework.zip
          key: libbox-${{ env.SING_BOX_VERSION }}-v3-${{ runner.os }} # 升级为v3让旧缓存失效

      - name: Set up Go 1.22
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install sagernet/gomobile toolchain
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        env:
          CGO_ENABLED: 1
        run: |
          go install -ldflags="-linkmode=external" -v github.com/sagernet/gomobile/cmd/gomobile@v0.1.4
          go install -ldflags="-linkmode=external" -v github.com/sagernet/gomobile/cmd/gobind@v0.1.4
          codesign -s - "$(go env GOPATH)/bin/gomobile"
          codesign -s - "$(go env GOPATH)/bin/gobind"
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: gomobile init
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        run: gomobile init

      - name: Clone sing-box
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        run: |
          git clone --branch ${{ env.SING_BOX_VERSION }} --depth 1 https://github.com/SagerNet/sing-box.git

      # 【修改处 2】：编译完立马打包成 zip，绝不把散件直接暴露给下游
      - name: Build and Zip Libbox.xcframework
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        working-directory: sing-box
        run: |
          make lib_ios
          mkdir -p ../libbox-output
          FRAMEWORK_PATH=$(find . -iname "Libbox.xcframework" -maxdepth 3 | head -n 1)
          if [ -z "$FRAMEWORK_PATH" ]; then
            echo "找不到 Libbox.xcframework，请检查 build 日志确认实际产出路径"
            find . -iname "*.xcframework"
            exit 1
          fi
          
          # 进入输出目录将其压缩为 zip，保留完整的 plist 与 macOS 特殊属性
          cd $(dirname "$FRAMEWORK_PATH")
          zip -r ../../../../libbox-output/Libbox.xcframework.zip $(basename "$FRAMEWORK_PATH")

      # 【修改处 3】：上传的是 zip 文件，而不是整个目录
      - name: Upload libbox artifact
        uses: actions/upload-artifact@v4
        with:
          name: libbox-archive
          path: libbox-output/Libbox.xcframework.zip
          retention-days: 7

  # ------------------------------------------------------------------
  # Job 2: 原有的 Flutter iOS 构建流程，加入 zip 解压与格式校验步骤
  # ------------------------------------------------------------------
  build:
    needs: build-libbox
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Cache Flutter pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Set build number
        run: |
          VERSION_NAME=$(grep '^version:' pubspec.yaml | sed 's/version: //' | cut -d '+' -f 1)
          sed -i.bak "s/^version: .*/version: ${VERSION_NAME}+${{ github.run_number }}/" pubspec.yaml
          cat pubspec.yaml | grep version

      # 【修改处 4】：下载之前压缩好的 zip 文件
      - name: Download libbox artifact
        uses: actions/download-artifact@v4
        with:
          name: libbox-archive
          path: ./temp-libbox

      # 【修改处 5】：将其解压到你的目标路径下（跟你项目中报错的路径对齐），且强制验证 plist 完整性
      - name: Extract and verify libbox framework
        run: |
          # 确保解压目标父路径存在，之前报错在 ios/Libbox.xcframework，所以解压到 ios/ 目录下
          mkdir -p ios
          unzip -o ./temp-libbox/Libbox.xcframework.zip -d ios/
          
          # 验证解压是否成功
          ls -la ios/Libbox.xcframework
          
          # 【双重保险】：使用系统工具检查解压出的 Info.plist 语法是否完全健康，防止 Xcode 编译直接变炸
          plutil -lint ios/Libbox.xcframework/Info.plist

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            ios/Pods
            ~/Library/Caches/CocoaPods
          key: ${{ runner.os }}-pods-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Cache DerivedData
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-derived-${{ hashFiles('**/*.swift', '**/*.m', '**/*.dart') }}
          restore-keys: |
            ${{ runner.os }}-derived-

      - name: Build Flutter iOS (no codesign)
        run: flutter build ios --release --no-codesign

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Install fastlane
        run: gem install fastlane

      - name: Run fastlane beta
        working-directory: ios
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
        run: fastlane beta