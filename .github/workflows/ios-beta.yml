name: iOS Beta Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  SING_BOX_VERSION: v1.10.7
  XCODE_VERSION: '16.4'

jobs:
  build-libbox:
    runs-on: macos-15
    timeout-minutes: 30
    outputs:
      cache-hit: ${{ steps.cache-libbox.outputs.cache-hit }}
    env:
      CGO_ENABLED: 1
      GOFLAGS: -ldflags=-linkmode=external
    steps:
      - name: Select Xcode ${{ env.XCODE_VERSION }}
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Cache libbox archive
        id: cache-libbox
        uses: actions/cache@v4
        with:
          path: libbox-output/Libbox.xcframework.zip
          key: libbox-${{ env.SING_BOX_VERSION }}-v5-${{ runner.os }}

      - name: Set up Go 1.22
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install sagernet/gomobile toolchain
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        env:
          CGO_ENABLED: 1
        run: |
          go install -ldflags="-linkmode=external" -v github.com/sagernet/gomobile/cmd/gomobile@v0.1.4
          go install -ldflags="-linkmode=external" -v github.com/sagernet/gomobile/cmd/gobind@v0.1.4
          codesign -s - "$(go env GOPATH)/bin/gomobile"
          codesign -s - "$(go env GOPATH)/bin/gobind"
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: gomobile init
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        run: gomobile init

      - name: Clone sing-box
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        run: |
          git clone --branch ${{ env.SING_BOX_VERSION }} --depth 1 https://github.com/SagerNet/sing-box.git

      - name: Build Libbox.xcframework
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        working-directory: sing-box
        run: |
          set -e
          make lib_ios
          FRAMEWORK_PATH=$(find . -iname "Libbox.xcframework" -maxdepth 3 | head -n 1)
          if [ -z "$FRAMEWORK_PATH" ]; then
            echo "找不到 Libbox.xcframework，请检查 build 日志确认实际产出路径"
            find . -iname "*.xcframework"
            exit 1
          fi
          echo "FRAMEWORK_PATH=$FRAMEWORK_PATH" >> "$GITHUB_ENV"

      - name: Debug Info.plist before zipping
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        working-directory: sing-box
        run: |
          echo "== Xcode 版本 =="
          xcodebuild -version
          echo "== 文件类型 =="
          file "$FRAMEWORK_PATH/Info.plist"
          echo "== plist 内容（转成 xml 方便阅读）=="
          plutil -convert xml1 -o - "$FRAMEWORK_PATH/Info.plist"
          echo "== 是否包含 AvailableLibraries 关键字段 =="
          if plutil -convert xml1 -o - "$FRAMEWORK_PATH/Info.plist" | grep -q "AvailableLibraries"; then
            echo "OK: 包含 AvailableLibraries"
          else
            echo "警告: 未找到 AvailableLibraries，这份 xcframework 很可能是不完整/格式不对的"
            exit 1
          fi

      - name: Archive Libbox.xcframework
        if: steps.cache-libbox.outputs.cache-hit != 'true'
        working-directory: sing-box
        run: |
          set -e
          mkdir -p $GITHUB_WORKSPACE/libbox-output
          cd $(dirname "$FRAMEWORK_PATH")
          zip -r -y $GITHUB_WORKSPACE/libbox-output/Libbox.xcframework.zip $(basename "$FRAMEWORK_PATH")
          cd $GITHUB_WORKSPACE
          unzip -t libbox-output/Libbox.xcframework.zip > /dev/null

      - name: Upload libbox artifact
        uses: actions/upload-artifact@v4
        with:
          name: libbox-archive
          path: libbox-output/Libbox.xcframework.zip
          retention-days: 7

  build:
    needs: build-libbox
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select Xcode ${{ env.XCODE_VERSION }}
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Cache Flutter pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Set build number
        run: |
          VERSION_NAME=$(grep '^version:' pubspec.yaml | sed 's/version: //' | cut -d '+' -f 1)
          sed -i.bak "s/^version: .*/version: ${VERSION_NAME}+${{ github.run_number }}/" pubspec.yaml
          cat pubspec.yaml | grep version

      - name: Download libbox artifact
        uses: actions/download-artifact@v4
        with:
          name: libbox-archive
          path: ./temp-libbox

      - name: Extract and verify libbox framework
        run: |
          set -e
          mkdir -p ios
          rm -rf ios/Libbox.xcframework
          unzip -q -o ./temp-libbox/Libbox.xcframework.zip -d ios/
          ls -la ios/Libbox.xcframework
          echo "== Xcode 版本（用于实际读取 xcframework 的这台 runner）=="
          xcodebuild -version
          echo "== 文件类型 =="
          file ios/Libbox.xcframework/Info.plist
          echo "== 语法检查 =="
          plutil -lint ios/Libbox.xcframework/Info.plist
          echo "== 语义检查：是否包含 AvailableLibraries =="
          if ! plutil -convert xml1 -o - ios/Libbox.xcframework/Info.plist | grep -q "AvailableLibraries"; then
            echo "错误: Info.plist 缺少 AvailableLibraries，xcframework 不完整"
            exit 1
          fi

      - name: Dump exact LibboxPlatformInterfaceProtocol requirements
        run: |
          echo "==================== 列出 xcframework 里所有的 .h 头文件 ===================="
          find ios/Libbox.xcframework -iname "*.h" -print
          echo "=========================================================================="

          echo "==================== 在所有头文件里搜索 PlatformInterfaceProtocol（不区分大小写，宽松匹配）===================="
          find ios/Libbox.xcframework -iname "*.h" -print0 | xargs -0 grep -l -i "PlatformInterfaceProtocol" || echo "没有任何头文件包含 PlatformInterfaceProtocol 字样"
          echo "=========================================================================="

          echo "==================== 逐个文件打印匹配到的协议定义上下文 ===================="
          for f in $(find ios/Libbox.xcframework -iname "*.h"); do
            if grep -q -i "PlatformInterfaceProtocol" "$f"; then
              echo "---- 命中文件: $f ----"
              grep -n -i -B2 -A 40 "@protocol.*PlatformInterfaceProtocol" "$f"
              echo "---- 命中文件结束 ----"
            fi
          done
          echo "=========================================================================="

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            ios/Pods
            ~/Library/Caches/CocoaPods
          key: ${{ runner.os }}-pods-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Cache DerivedData
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-derived-${{ hashFiles('**/*.swift', '**/*.m', '**/*.dart') }}
          restore-keys: |
            ${{ runner.os }}-derived-

      # 【修正】之前用 --config-only 是错的：它只做 pod install / 生成配置，
      # 并不会触发 Xcode 里 Flutter 的 Run Script 构建阶段（真正把引擎 Flutter.framework
      # 拷贝进 ios/Flutter/ephemeral 的正是这个脚本，只有在 target 真正被 build 时才会跑，
      # 而且这个 Run Script 挂在 Runner 主 target 上，不是 PacketTunnelExtension 上）。
      # 所以必须先跑一次完整的 flutter build ios，让 Runner 主 target 把引擎产物准备好，
      # 之后再单独 build 扩展 target 才能找到 Flutter 模块。
      - name: Build Flutter iOS (no codesign)
        run: flutter build ios --release --no-codesign

      # 【关键修改】删掉了末尾的 || true。一旦协议不吻合，直接在这里爆红中断！
      # 放在 flutter build ios 之后执行：Flutter 引擎已就位，这一步基本是增量重建，
      # 主要用来把扩展 target 编译时未截断、未压制的完整错误信息打印出来。
      - name: Debug Swift Extension with raw xcodebuild
        run: |
          cd ios
          xcodebuild -workspace Runner.xcworkspace -scheme PacketTunnelExtension -configuration Release build CODE_SIGNING_ALLOWED=NO

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Install fastlane
        run: gem install fastlane

      - name: Run fastlane beta
        working-directory: ios
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
        run: fastlane beta